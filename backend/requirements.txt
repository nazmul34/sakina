# Sakina backend dependencies
# Django 4.2 LTS (supports Python 3.8–3.12; 5.x requires 3.10+)
Django==4.2.20
djangorestframework==3.15.2
django-environ==0.11.2
django-cors-headers==4.4.0

# WSGI server for production / docker
gunicorn==23.0.0

# PostgreSQL driver (used when DATABASE_URL points at Postgres; SQLite is the dev default)
psycopg2-binary==2.9.10
